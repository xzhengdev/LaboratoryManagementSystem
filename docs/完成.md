已完成改造

后端接入预约幂等键（Idempotency-Key）并持久化幂等记录，防重复创建。
reservation_service.py
reservations.py
entities.py

后端接入分布式锁服务（按“实验室+日期”串行化关键写路径），用于预约创建与审批通过冲突控制。
distributed_lock_service.py
reservation_service.py

增加 Redis 配置与初始化能力（可开关）。
config.py
extensions.py
init.py

补充模型导出、依赖、迁移脚本、环境变量与说明文档。
requirements.txt
.env.example
20260427_add_idempotency_and_reservation_indexes.sql
README.md

Web/小程序预约请求加入 Idempotency-Key，并加前端防重复提交状态。
index.js
request.js
reserve.vue
api.js
request.js
reserve.js

你要的说明文档已在仓库内。
分布式与高并发改造实施说明.md

已做验证

python -m compileall app scripts 通过。
实测同一 Idempotency-Key 连续提交两次，只创建一条预约，返回同一 id（mid=12, after=12, id1=id2=49）。
你现在启用分布式锁要做

安装新依赖：pip install -r backend/requirements.txt
配置 backend/.env：REDIS_URL、ENABLE_DISTRIBUTED_LOCK=1
执行迁移 SQL（或重建库后 seed）。
说明
我在本地验证时执行了 seed_data() 和幂等测试，当前开发库里新增了测试预约数据。

下一步建议

继续做阶段B：限流 + 缓存（排期/统计）+ 错误码规范。
同时补压测脚本（并发抢同一时段）和结果落盘，直接给论文/PPT用。