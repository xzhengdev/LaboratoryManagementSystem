<view class="page">
  <view class="container">
    <view class="login-main">
      <view class="brand-block">
        <view class="brand-logo-wrap">
          <image class="brand-logo" src="/assets/logo.png" mode="aspectFit" />
        </view>
        <text class="brand-title">{{ui.brandTitle}}</text>
        <text class="brand-subtitle">{{ui.brandSubtitle}}</text>
      </view>
      <view class="login-card">
        <view class="card-title-wrap">
          <text class="card-title-main">{{ui.login}}</text>
        </view>
        <view class="card-fields">
          <view class="form-group">
            <view class="input-shell">
              <text class="input-icon">{{ui.idIcon}}</text>
              <input class="input" placeholder="{{ui.accountPlaceholder}}" placeholder-class="placeholder" bindinput="onUsername" value="{{form.username}}" confirm-type="next" />
            </view>
          </view>
          <view class="form-group">
            <view class="input-shell">
              <text class="input-icon">{{ui.pwdIcon}}</text>
              <input class="input" password placeholder="{{ui.passwordPlaceholder}}" placeholder-class="placeholder" bindinput="onPassword" value="{{form.password}}" confirm-type="done" />
            </view>
          </view>
        </view>
        <view class="card-actions">
          <view class="assist-row">
            <view class="remember-wrap" bindtap="toggleRemember">
              <view class="remember-dot {{rememberPassword ? 'active' : ''}}"></view>
              <text class="remember-text">{{ui.remember}}</text>
            </view>
            <text class="forgot" bindtap="onForgotPassword">{{ui.forgot}}</text>
          </view>
          <button class="submit" loading="{{loading}}" disabled="{{loading}}" bindtap="doLogin">
            {{loading ? ui.loggingIn : ui.login}}
          </button>
        </view>
      </view>
    </view>
    <view class="login-page__bottom">
      <view class="login-footer">
        <view class="login-footer__line"></view>
        <text class="login-footer__text">{{ui.secureText}}</text>
      </view>
    </view>
  </view>
</view>
